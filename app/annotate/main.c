#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

//#include <libxml/parser.h>
//#include <libxml/tree.h>

#include "base64.h"




void main() {
    /* xmlDocPtr doc = xmlReadFile("rom.ann", NULL, 0); */
    /* FILE* fp = fopen("rom.sfc", "r"); */
    /* if(doc == 0 || fp == 0) return; */

    /* xmlNodePtr cur = xmlDocGetRootElement(doc); */
    /* if(!strcmp(cur->name, "annotate")) { */
    /*     printf("Not a valid annotate file\n"); */
    /*     return; */
    /* } */
    /* cur = cur->xmlChildrenNode; */
    /* while (cur != NULL) { */
    /*     if(!strcmp(cur->name, "asm-r65816")) */
    /*         printf("%s\n", xmlNodeListGetString(doc, cur->xmlChildrenNode, 1)); */
    /*     if(!strcmp(cur->name, "data")) */
    /*         printf("%s\n", xmlNodeListGetString(doc, cur->xmlChildrenNode, 1)); */
    /*     if(!strcmp(cur->name, "comment")) */
    /*         printf("%s\n", xmlNodeListGetString(doc, cur->xmlChildrenNode, 1)); */

    /*     cur = cur->next; */
    /* } */
    
}
